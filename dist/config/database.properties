# Database Configuration
# Using H2 Database (in-memory for testing, file-based for persistence)
db.url=jdbc:h2:./data/keybase;AUTO_SERVER=TRUE
db.username=sa
db.password=
db.driverClassName=org.h2.Driver